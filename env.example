# LLM Analysis Quiz - Environment Variables

# Required: Your email and secret (as submitted in Google Form)
EMAIL=your-email@example.com
SECRET=your-secret-string

# Required: LLM API Configuration
# For OpenAI API:
LLM_API_KEY=sk-your-openai-api-key
LLM_MODEL=gpt-4o-mini
# LLM_BASE_URL=  # Leave empty for OpenAI, or set for custom endpoint (e.g., OpenRouter)

# Optional: For custom LLM endpoints (like OpenRouter, AI Pipe, etc.)
# LLM_BASE_URL=https://openrouter.ai/api/v1
# LLM_MODEL=openai/gpt-4o-mini

# Optional: HTTP Settings
# HTTP_TIMEOUT=60
# MAX_PAYLOAD_BYTES=1000000
# LOG_LEVEL=INFO
# USER_AGENT=TDS-LLM-Analysis-Quiz-Bot/1.0

